---
name: traveler-ui
description: 관광객(Traveler) 사용자 화면 UI/UX 구현
status: backlog
created: 2025-12-04T12:30:00Z
depends_on: [travel-web-system]
---

# PRD: 관광객(Traveler) UI

## Executive Summary

한국 방문 외국인 관광객을 위한 프론트엔드 UI를 구현합니다. 에어비앤비 스타일의 모던하고 직관적인 디자인으로, 상품 탐색부터 예약 완료까지 매끄러운 사용자 경험을 제공합니다.

**디자인 원칙:**
- 에어비앤비 스타일의 클린하고 모던한 UI
- 모바일 퍼스트 반응형 디자인
- Blade + Vue.js 하이브리드 구조
- 재사용 가능한 컴포넌트 시스템

---

## Tech Stack & Dependencies

### 프론트엔드 기술 스택

| 기술 | 버전 | 용도 |
|-----|------|------|
| Vue.js | 3.x | 인터랙티브 컴포넌트 |
| Tailwind CSS | 3.x | 유틸리티 기반 스타일링 |
| Vite | 5.x | 빌드 도구 |
| Axios | 1.x | HTTP 클라이언트 |
| Lodash | 4.x | 유틸리티 함수 |
| Pinia | 2.x | Vue 상태 관리 |

### NPM 패키지

```json
{
  "dependencies": {
    "vue": "^3.4",
    "pinia": "^2.1",
    "axios": "^1.6",
    "lodash": "^4.17",
    "@vueuse/core": "^10.7"
  },
  "devDependencies": {
    "tailwindcss": "^3.4",
    "autoprefixer": "^10.4",
    "postcss": "^8.4",
    "@vitejs/plugin-vue": "^5.0",
    "vite": "^5.0"
  }
}
```

### Tailwind CSS 설정

```js
// tailwind.config.js
module.exports = {
  content: [
    './resources/**/*.blade.php',
    './resources/**/*.js',
    './resources/**/*.vue',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#FFF0F3',
          100: '#FFE4E9',
          200: '#FFCCD5',
          300: '#FFA3B5',
          400: '#FF7A95',
          500: '#FF385C',  // 메인 컬러
          600: '#E31C5F',
          700: '#C91454',
          800: '#A81247',
          900: '#8B103D',
        },
      },
      fontFamily: {
        sans: ['Pretendard', '-apple-system', 'BlinkMacSystemFont', 'Segoe UI', 'sans-serif'],
      },
      boxShadow: {
        'card': '0 6px 16px rgba(0,0,0,0.12)',
        'card-hover': '0 6px 20px rgba(0,0,0,0.2)',
        'dropdown': '0 2px 16px rgba(0,0,0,0.12)',
        'modal': '0 8px 28px rgba(0,0,0,0.28)',
      },
      borderRadius: {
        'xl': '12px',
        '2xl': '16px',
        '3xl': '24px',
      },
    },
  },
  plugins: [
    require('@tailwindcss/forms'),
    require('@tailwindcss/typography'),
    require('@tailwindcss/aspect-ratio'),
  ],
}
```

### Axios 설정

```js
// resources/js/plugins/axios.js
import axios from 'axios';

const api = axios.create({
  baseURL: '/api',
  headers: {
    'Content-Type': 'application/json',
    'Accept': 'application/json',
    'X-Requested-With': 'XMLHttpRequest',
  },
  withCredentials: true,
});

// CSRF 토큰 자동 설정
api.interceptors.request.use(config => {
  const token = document.querySelector('meta[name="csrf-token"]')?.content;
  if (token) {
    config.headers['X-CSRF-TOKEN'] = token;
  }
  return config;
});

// 응답 인터셉터 (에러 핸들링)
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      window.location.href = '/login';
    }
    if (error.response?.status === 422) {
      // 유효성 검사 에러 처리
    }
    return Promise.reject(error);
  }
);

export default api;
```

### Lodash 사용 예시

```js
// 자주 사용하는 lodash 함수 (개별 import로 번들 크기 최적화)
import debounce from 'lodash/debounce';
import throttle from 'lodash/throttle';
import get from 'lodash/get';
import isEmpty from 'lodash/isEmpty';
import cloneDeep from 'lodash/cloneDeep';

// 검색 입력 디바운스
const handleSearch = debounce((query) => {
  api.get('/products', { params: { keyword: query } });
}, 300);

// 스크롤 이벤트 쓰로틀
const handleScroll = throttle(() => {
  // 무한 스크롤 로직
}, 200);
```

### Vue App 엔트리포인트

```js
// resources/js/app.js
import { createApp } from 'vue';
import { createPinia } from 'pinia';
import api from './plugins/axios';

// 전역 컴포넌트
import Toast from './components/common/Toast.vue';
import Modal from './components/common/Modal.vue';
import WishlistButton from './components/common/WishlistButton.vue';

// 페이지별 컴포넌트 (동적 로드)
const BookingWidget = () => import('./components/booking/BookingWidget.vue');
const ImageGallery = () => import('./components/product/ImageGallery.vue');
const SearchBar = () => import('./components/search/SearchBar.vue');

const app = createApp({});
const pinia = createPinia();

// 플러그인
app.use(pinia);

// 전역 속성
app.config.globalProperties.$api = api;

// 전역 컴포넌트 등록
app.component('Toast', Toast);
app.component('Modal', Modal);
app.component('WishlistButton', WishlistButton);

// 비동기 컴포넌트 등록
app.component('BookingWidget', BookingWidget);
app.component('ImageGallery', ImageGallery);
app.component('SearchBar', SearchBar);

// 마운트
app.mount('#app');
```

### Vite 설정

```js
// vite.config.js
import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';
import vue from '@vitejs/plugin-vue';

export default defineConfig({
  plugins: [
    laravel({
      input: [
        'resources/css/app.css',
        'resources/js/app.js',
      ],
      refresh: true,
    }),
    vue({
      template: {
        transformAssetUrls: {
          base: null,
          includeAbsolute: false,
        },
      },
    }),
  ],
  resolve: {
    alias: {
      '@': '/resources/js',
    },
  },
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor': ['vue', 'pinia', 'axios'],
          'lodash': ['lodash/debounce', 'lodash/throttle', 'lodash/get'],
        },
      },
    },
  },
});
```

---

## Design System

### 컬러 팔레트

```css
/* Primary Colors */
--primary-500: #FF385C;     /* 에어비앤비 핑크 - 주요 액션 */
--primary-600: #E31C5F;     /* 호버 상태 */
--primary-50: #FFF0F3;      /* 배경 하이라이트 */

/* Neutral Colors */
--gray-900: #222222;        /* 메인 텍스트 */
--gray-700: #484848;        /* 서브 텍스트 */
--gray-500: #717171;        /* 보조 텍스트 */
--gray-300: #DDDDDD;        /* 보더 */
--gray-100: #F7F7F7;        /* 배경 */
--white: #FFFFFF;

/* Semantic Colors */
--success: #008A05;         /* 성공/예약확정 */
--warning: #FFB400;         /* 경고/대기중 */
--error: #C13515;           /* 에러/취소 */
--info: #428BFF;            /* 정보 */
```

### 타이포그래피

```css
/* Font Family */
font-family: 'Pretendard', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;

/* Font Sizes */
--text-xs: 12px;
--text-sm: 14px;
--text-base: 16px;
--text-lg: 18px;
--text-xl: 20px;
--text-2xl: 24px;
--text-3xl: 30px;
--text-4xl: 36px;

/* Font Weights */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
--font-extrabold: 800;
```

### 그림자 & 둥근 모서리

```css
/* Shadows */
--shadow-sm: 0 1px 2px rgba(0,0,0,0.08);
--shadow-md: 0 4px 12px rgba(0,0,0,0.08);
--shadow-lg: 0 8px 28px rgba(0,0,0,0.12);
--shadow-xl: 0 16px 40px rgba(0,0,0,0.16);

/* Border Radius */
--rounded-sm: 8px;
--rounded-md: 12px;
--rounded-lg: 16px;
--rounded-xl: 24px;
--rounded-full: 9999px;
```

---

## 공통 컴포넌트

### 1. UI 컴포넌트 (Blade)

| 컴포넌트 | 파일명 | 용도 |
|---------|--------|------|
| Modal | `components/ui/modal.blade.php` | 모달 다이얼로그 |
| Dropdown | `components/ui/dropdown.blade.php` | 드롭다운 메뉴 |
| Toast | `components/ui/toast.blade.php` | 토스트 알림 |
| Alert | `components/ui/alert.blade.php` | 인라인 알림 |
| Button | `components/ui/button.blade.php` | 버튼 스타일 |
| Input | `components/ui/input.blade.php` | 입력 필드 |
| Select | `components/ui/select.blade.php` | 셀렉트 박스 |
| Card | `components/ui/card.blade.php` | 카드 컨테이너 |
| Badge | `components/ui/badge.blade.php` | 상태 뱃지 |
| Avatar | `components/ui/avatar.blade.php` | 사용자 아바타 |
| Skeleton | `components/ui/skeleton.blade.php` | 로딩 스켈레톤 |
| Pagination | `components/ui/pagination.blade.php` | 페이지네이션 |

### 2. 레이아웃 컴포넌트

| 컴포넌트 | 파일명 | 용도 |
|---------|--------|------|
| AppLayout | `components/layouts/app.blade.php` | 메인 레이아웃 (수정) |
| Header | `components/layouts/header.blade.php` | 헤더/네비게이션 |
| Footer | `components/layouts/footer.blade.php` | 푸터 |
| MobileNav | `components/layouts/mobile-nav.blade.php` | 모바일 하단 네비게이션 |

### 3. Vue.js 컴포넌트

| 컴포넌트 | 파일명 | 용도 |
|---------|--------|------|
| SearchBar | `js/components/search/SearchBar.vue` | 검색바 (자동완성) |
| DatePicker | `js/components/booking/DatePicker.vue` | 날짜 선택기 |
| GuestSelector | `js/components/booking/GuestSelector.vue` | 인원 선택기 |
| BookingForm | `js/components/booking/BookingForm.vue` | 예약 폼 |
| ImageGallery | `js/components/product/ImageGallery.vue` | 이미지 갤러리 |
| ReviewList | `js/components/review/ReviewList.vue` | 리뷰 목록 |
| WishlistButton | `js/components/common/WishlistButton.vue` | 위시리스트 버튼 |
| ShareButton | `js/components/common/ShareButton.vue` | 공유 버튼 |

---

## 화면별 상세 명세

### 1. 메인 페이지 (Home)

**경로:** `/` , `/{locale}`
**파일:** `resources/views/home.blade.php`

#### 레이아웃 구조

```
┌─────────────────────────────────────────────┐
│  Header (Fixed)                              │
│  - 로고 / 검색바 / 언어 / 로그인             │
├─────────────────────────────────────────────┤
│  Hero Section                                │
│  - 풀스크린 배경 이미지/영상                 │
│  - 메인 카피 + 검색 위젯                     │
├─────────────────────────────────────────────┤
│  카테고리 네비게이션 (가로 스크롤)           │
│  - 아이콘 + 라벨                             │
├─────────────────────────────────────────────┤
│  추천 상품 섹션                              │
│  - 상품 카드 그리드 (4열)                    │
├─────────────────────────────────────────────┤
│  지역별 탐색                                 │
│  - 지역 이미지 카드 (6열)                    │
├─────────────────────────────────────────────┤
│  인기 체험 섹션                              │
│  - 가로 스크롤 카드                          │
├─────────────────────────────────────────────┤
│  Footer                                      │
└─────────────────────────────────────────────┘
```

#### 주요 기능

- **Hero 검색 위젯**
  - 위치/날짜/인원 선택
  - 에어비앤비 스타일 확장형 검색바

- **카테고리 필터**
  - 아이콘 기반 가로 스크롤
  - 선택 시 상품 목록으로 이동

- **상품 카드**
  - 이미지 캐러셀 (스와이프)
  - 위시리스트 하트 버튼
  - 평점/리뷰 수 표시
  - 가격 표시

---

### 2. 상품 목록 페이지 (Products List)

**경로:** `/{locale}/products`
**파일:** `resources/views/traveler/products/index.blade.php`

#### 레이아웃 구조

```
┌─────────────────────────────────────────────┐
│  Header (Sticky)                             │
├─────────────────────────────────────────────┤
│  필터 바 (Sticky)                            │
│  - 카테고리 | 가격 | 날짜 | 지역 | 더보기    │
├─────────────────────────────────────────────┤
│  지도 토글 버튼 (모바일)                     │
├──────────────────────┬──────────────────────┤
│  상품 그리드         │  지도 (토글)          │
│  - 반응형 그리드     │  - 마커 클러스터      │
│  - 무한 스크롤       │  - 가격 표시 마커     │
│                      │                      │
└──────────────────────┴──────────────────────┘
```

#### 필터 옵션

| 필터 | 타입 | 값 |
|-----|------|-----|
| 유형 | 드롭다운 | 투어, 액티비티, 티켓, 패키지 |
| 지역 | 드롭다운 | 서울, 부산, 제주, 경주... |
| 가격 | 레인지 슬라이더 | 0 ~ 500,000원 |
| 날짜 | 캘린더 | 단일/범위 선택 |
| 인원 | 스텝퍼 | 성인/아동 |
| 언어 | 체크박스 | 영어, 한국어, 중국어, 일본어 |
| 평점 | 버튼 그룹 | 4.0+, 4.5+, 4.8+ |
| 정렬 | 드롭다운 | 추천순, 가격순, 평점순, 최신순 |

#### 상품 카드 디자인

```
┌─────────────────────────┐
│  ┌───────────────────┐  │
│  │                   │  │  ← 이미지 캐러셀
│  │     Image         │♡ │  ← 위시리스트 버튼
│  │                   │  │
│  │  • • • • •       │  │  ← 페이지 인디케이터
│  └───────────────────┘  │
│  ★ 4.92 (128)           │  ← 평점/리뷰수
│  전주 한옥마을 당일투어   │  ← 상품명
│  전주 · 투어             │  ← 지역/유형
│  ₩45,000 /인            │  ← 가격
└─────────────────────────┘
```

---

### 3. 상품 상세 페이지 (Product Detail)

**경로:** `/{locale}/products/{id}`
**파일:** `resources/views/traveler/products/show.blade.php`

#### 레이아웃 구조 (데스크톱)

```
┌─────────────────────────────────────────────┐
│  Header                                      │
├─────────────────────────────────────────────┤
│  이미지 갤러리 (그리드 5장)                  │
│  ┌─────────────┬─────────┬─────────┐        │
│  │             │         │         │        │
│  │   Main      │  Sub 1  │  Sub 2  │        │
│  │   Image     ├─────────┼─────────┤        │
│  │             │  Sub 3  │  Sub 4  │        │
│  └─────────────┴─────────┴─────────┘        │
├─────────────────────────────────────────────┤
│  ┌────────────────────┐ ┌─────────────────┐ │
│  │  상품 정보          │ │  예약 위젯      │ │
│  │  - 제목/평점        │ │  (Sticky)       │ │
│  │  - 호스트 정보      │ │  - 가격         │ │
│  │  - 하이라이트       │ │  - 날짜 선택    │ │
│  │  - 상세 설명        │ │  - 인원 선택    │ │
│  │  - 포함/불포함      │ │  - 예약 버튼    │ │
│  │  - 일정표           │ └─────────────────┘ │
│  │  - 위치/지도        │                     │
│  │  - 유의사항         │                     │
│  └────────────────────┘                     │
├─────────────────────────────────────────────┤
│  리뷰 섹션                                   │
│  - 평점 요약 / 리뷰 목록                     │
├─────────────────────────────────────────────┤
│  비슷한 상품 추천                            │
├─────────────────────────────────────────────┤
│  Footer                                      │
└─────────────────────────────────────────────┘
```

#### 레이아웃 구조 (모바일)

```
┌─────────────────────┐
│  Header             │
├─────────────────────┤
│  이미지 스와이프     │
│  (풀 와이드)         │
├─────────────────────┤
│  상품 정보          │
│  - 제목/평점        │
│  - 호스트           │
│  - 상세 내용        │
├─────────────────────┤
│  리뷰               │
├─────────────────────┤
│  유사 상품          │
├─────────────────────┤
│  Footer             │
├─────────────────────┤
│  예약 바 (Fixed)    │
│  가격 | [예약하기]  │
└─────────────────────┘
```

#### 예약 위젯 (Vue Component)

```vue
<!-- BookingWidget.vue -->
<template>
  <div class="booking-widget">
    <div class="price">
      <span class="amount">₩{{ formatPrice(price) }}</span>
      <span class="unit">/인</span>
    </div>

    <!-- 날짜 선택 -->
    <div class="date-picker" @click="openDatePicker">
      <div class="label">날짜</div>
      <div class="value">{{ selectedDate || '날짜 선택' }}</div>
    </div>

    <!-- 인원 선택 -->
    <div class="guest-selector" @click="openGuestSelector">
      <div class="label">인원</div>
      <div class="value">성인 {{ adults }}명, 아동 {{ children }}명</div>
    </div>

    <!-- 총 금액 -->
    <div class="total">
      <span>총 금액</span>
      <span>₩{{ formatPrice(totalPrice) }}</span>
    </div>

    <!-- 예약 버튼 -->
    <button class="btn-primary" @click="submitBooking">
      {{ isInstant ? '바로 예약하기' : '예약 요청하기' }}
    </button>

    <p class="note">예약 확정 전에는 요금이 청구되지 않습니다.</p>
  </div>
</template>
```

---

### 4. 예약 페이지 (Booking)

**경로:** `/{locale}/bookings/create?product_id={id}`
**파일:** `resources/views/traveler/bookings/create.blade.php`

#### 레이아웃 구조

```
┌─────────────────────────────────────────────┐
│  Header (간소화)                             │
├─────────────────────────────────────────────┤
│  Progress Bar (Step 1/3)                     │
├──────────────────────┬──────────────────────┤
│  예약 정보 입력      │  주문 요약            │
│                      │                      │
│  1. 날짜 확인        │  ┌────────────────┐  │
│  2. 인원 확인        │  │  상품 이미지    │  │
│  3. 옵션 선택        │  │  상품명         │  │
│  4. 예약자 정보      │  │  날짜/인원      │  │
│     - 이름           │  │  ────────────  │  │
│     - 연락처         │  │  가격 상세      │  │
│     - 이메일         │  │  - 성인 x2     │  │
│  5. 요청사항         │  │  - 아동 x1     │  │
│                      │  │  - 옵션        │  │
│  [예약 확정하기]     │  │  ────────────  │  │
│                      │  │  총 금액       │  │
│                      │  └────────────────┘  │
└──────────────────────┴──────────────────────┘
```

#### 예약 완료 페이지

```
┌─────────────────────────────────────────────┐
│                                             │
│          ✓ 예약이 완료되었습니다            │
│                                             │
│  예약번호: BK-2024-XXXXXX                   │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  예약 상세 정보                        │  │
│  │  - 상품명                              │  │
│  │  - 날짜/시간                           │  │
│  │  - 인원                                │  │
│  │  - 금액                                │  │
│  │  - 상태: 확정 / 승인 대기              │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  [예약 내역 보기]  [홈으로]                 │
│                                             │
└─────────────────────────────────────────────┘
```

---

### 5. 마이페이지 (My Page)

**경로:** `/{locale}/my`
**파일:** `resources/views/traveler/my/index.blade.php`

#### 탭 구조

| 탭 | 경로 | 내용 |
|---|------|------|
| 예약 내역 | `/my/bookings` | 예약 목록 (예정/완료/취소) |
| 위시리스트 | `/my/wishlist` | 찜한 상품 목록 |
| 리뷰 | `/my/reviews` | 작성한 리뷰 목록 |
| 메시지 | `/my/messages` | 메시지함 |
| 프로필 | `/my/profile` | 프로필 수정 |

#### 예약 내역 카드

```
┌─────────────────────────────────────────────┐
│  ┌────┐  전주 한옥마을 당일투어              │
│  │Img │  2024.12.25 (수) · 성인 2, 아동 1   │
│  └────┘  ────────────────────────────────── │
│          예약번호: BK-2024-001234           │
│          상태: [확정] 또는 [대기중]          │
│          ────────────────────────────────── │
│          [상세보기] [취소하기] [리뷰쓰기]    │
└─────────────────────────────────────────────┘
```

---

### 6. 인증 페이지

#### 로그인 모달

```
┌─────────────────────────────────────────────┐
│                    ✕                        │
│                                             │
│           로그인 또는 회원가입               │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  📧 이메일 주소                        │  │
│  └───────────────────────────────────────┘  │
│                                             │
│  [계속하기]                                 │
│                                             │
│  ──────────── 또는 ────────────            │
│                                             │
│  [🔴 Google로 계속하기]                     │
│  [💬 카카오로 계속하기]                     │
│  [🍎 Apple로 계속하기]                      │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 인터랙션 & 애니메이션

### 1. 페이지 전환
- Turbo/Livewire 사용 고려
- 페이지 로드 시 부드러운 페이드인

### 2. 호버 효과
- 카드: 약간 올라오며 그림자 확대
- 버튼: 배경색 변화 (0.2s ease)
- 이미지: scale(1.05) 확대

### 3. 스크롤 애니메이션
- 섹션 진입 시 fade-up
- 무한 스크롤 로딩 스피너

### 4. 모달/드롭다운
- 열림: scale(0.95) → scale(1) + fade
- 닫힘: fade out
- 백드롭: 블러 효과

### 5. 토스트 알림
- 우측 하단에서 슬라이드인
- 3초 후 자동 사라짐
- 스와이프로 닫기 (모바일)

---

## 반응형 브레이크포인트

| 이름 | 너비 | 적용 |
|------|------|------|
| Mobile | 0 ~ 639px | 1열 그리드, 하단 고정 네비 |
| Tablet | 640 ~ 1023px | 2열 그리드 |
| Desktop | 1024 ~ 1279px | 3열 그리드 |
| Wide | 1280px+ | 4열 그리드, 지도 사이드바 |

---

## 접근성 (a11y)

- 모든 이미지에 alt 텍스트
- 키보드 네비게이션 지원
- ARIA 라벨 적용
- 충분한 색상 대비 (WCAG 2.1 AA)
- 포커스 표시 (outline)

---

## 성능 최적화

### 이미지
- WebP 포맷 우선
- 썸네일/원본 분리
- Lazy loading
- Placeholder blur

### 번들
- 코드 스플리팅
- Vue 컴포넌트 비동기 로드
- Critical CSS 인라인

### 캐싱
- 정적 자원 CDN
- API 응답 캐싱
- Service Worker (PWA 대비)

---

## 다국어 지원

### URL 구조
```
/{locale}/products
/{locale}/products/{id}
/{locale}/my/bookings
```

### 언어 전환
- 헤더 드롭다운
- 현재 페이지 유지하며 locale만 변경
- 쿠키에 선호 언어 저장

---

## 파일 구조

```
resources/
├── views/
│   ├── components/
│   │   ├── ui/                    # UI 컴포넌트
│   │   │   ├── modal.blade.php
│   │   │   ├── dropdown.blade.php
│   │   │   ├── toast.blade.php
│   │   │   ├── alert.blade.php
│   │   │   ├── button.blade.php
│   │   │   ├── input.blade.php
│   │   │   ├── select.blade.php
│   │   │   ├── card.blade.php
│   │   │   ├── badge.blade.php
│   │   │   ├── avatar.blade.php
│   │   │   ├── skeleton.blade.php
│   │   │   └── pagination.blade.php
│   │   ├── layouts/               # 레이아웃
│   │   │   ├── app.blade.php
│   │   │   ├── header.blade.php
│   │   │   ├── footer.blade.php
│   │   │   └── mobile-nav.blade.php
│   │   └── product/               # 상품 관련
│   │       ├── card.blade.php
│   │       └── filter.blade.php
│   ├── traveler/
│   │   ├── products/
│   │   │   ├── index.blade.php    # 상품 목록
│   │   │   └── show.blade.php     # 상품 상세
│   │   ├── bookings/
│   │   │   ├── create.blade.php   # 예약 생성
│   │   │   ├── show.blade.php     # 예약 상세
│   │   │   └── complete.blade.php # 예약 완료
│   │   └── my/
│   │       ├── index.blade.php    # 마이페이지
│   │       ├── bookings.blade.php # 예약 내역
│   │       ├── wishlist.blade.php # 위시리스트
│   │       ├── reviews.blade.php  # 리뷰
│   │       └── profile.blade.php  # 프로필
│   ├── auth/
│   │   ├── login.blade.php
│   │   └── register.blade.php
│   └── home.blade.php             # 메인 페이지
├── js/
│   ├── app.js
│   ├── components/
│   │   ├── common/
│   │   │   ├── WishlistButton.vue
│   │   │   ├── ShareButton.vue
│   │   │   └── Toast.vue
│   │   ├── search/
│   │   │   └── SearchBar.vue
│   │   ├── booking/
│   │   │   ├── BookingWidget.vue
│   │   │   ├── DatePicker.vue
│   │   │   └── GuestSelector.vue
│   │   ├── product/
│   │   │   ├── ImageGallery.vue
│   │   │   └── ProductCard.vue
│   │   └── review/
│   │       ├── ReviewList.vue
│   │       └── ReviewForm.vue
│   └── stores/
│       ├── auth.js
│       ├── booking.js
│       └── wishlist.js
└── css/
    └── app.css
```

---

## Tasks Preview

에픽 분해 시 생성될 태스크 목록:

| # | 태스크 | 규모 |
|---|--------|------|
| 001 | 디자인 시스템 및 공통 컴포넌트 | M |
| 002 | 레이아웃 컴포넌트 (Header, Footer, Mobile Nav) | M |
| 003 | 메인 페이지 리뉴얼 | L |
| 004 | 상품 목록 페이지 | L |
| 005 | 상품 상세 페이지 | L |
| 006 | 예약 플로우 (생성/완료) | M |
| 007 | 마이페이지 (예약내역/위시리스트/리뷰) | M |
| 008 | 인증 UI (로그인/회원가입) | S |
| 009 | 반응형 최적화 및 테스트 | M |

---

## Success Criteria

### 기능 완료
- [ ] 모든 화면 구현 완료
- [ ] 반응형 레이아웃 (모바일/태블릿/데스크톱)
- [ ] 다국어 지원 (한/영)

### 디자인 품질
- [ ] 에어비앤비 스타일 일관성
- [ ] 애니메이션/인터랙션 자연스러움
- [ ] Lighthouse 성능 점수 80+

### 사용자 경험
- [ ] 예약 플로우 3분 내 완료 가능
- [ ] 직관적인 네비게이션
- [ ] 빠른 페이지 로드 (3초 이내)